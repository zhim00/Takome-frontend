# 生产构建配置，vite build 时自动加载（mode=production）。
#
# 必须写完整域名：apiClient.ts 用 new URL(path, API_BASE) 拼接，base 需要是绝对 URL。
# 不要写成 /api，前端请求路径本身已经是 /api/front/...
#
# 这个值在 pnpm build:novel 时编译进产物，改动后必须重新构建才生效，
# 改服务器的 .env 或重新 docker build 都不会影响已有产物。
VITE_API_BASE_URL=https://takome.top
